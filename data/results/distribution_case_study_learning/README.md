# Stepwise Nike distribution case-study results

Run `research/nike_distribution_case_study.ipynb` to refresh this directory. The notebook is saved without executed outputs and reads the current preprocessing manifest before loading its case-study files. A run now also writes the normalized daily equity-price table, sourced event ledger, and black-background price/event chart.

Expected outputs include Nike channel calculations, normalized wholesale comparisons, Foot Locker vendor concentration, evidence and limitation matrices, a monitoring scorecard, six figures and a run manifest. The notebook does not produce a causal estimate or change the DCF.
